# 融景 路线图

> 跨阶段开发计划，每个阶段完成后更新状态。

**结构说明（2026-07-21 改造）**：本文件只保留「当前主线状态 + 待确认区 + 未排期 + 已关闭索引」；判断项目现在走到哪一步只看本文件，不需要通读全文。每条已关闭 / 待确认专项的详细背景、验收标准、踩坑记录下沉到 `docs/roadmap/`（已关闭）与 `docs/roadmap/pending/`（待确认）子文件。**子文件不写新状态、不写新进度、不写下一步**——文中残留的 ✅/🚧/⏸ 等状态记号只是下沉时的原文快照，判断当前状态只认本文件。

---

## 当前主线状态

（本次迁移时点：全部专项均已收口为「已关闭」或「待确认」，暂无仍在推进中的当前主线项。）

---

## 待确认区（等用户裁决：还在做 / 已关闭 / 废弃归档）

（空——2026-07-21 用户裁决 V3.8 三列布局已完工，转入已关闭区。）

---

## 未排期

- P3.B — 自动角点识别后续扩展方向 ⏸（原「第三期 P3」标注「待评估」，未启动）：SAM（Segment Anything Model）集成、批量模板创建（选一批背景图自动检测角点批量生成模板）、与 AI 背景生成联动一步到位。三项均未排入具体阶段，详情见 [原文快照 →](roadmap/p3a-auto-corner-detection.md)（该文件同时含已关闭的 P3.A）

---

## 已关闭

- 拼图上大图布局 + 模板批量删除入口显化 ✅ `73c5e4fcaa1ce95b5daecd189952c17c43adb283` — 验证: `python3 _test_collage.py`、`python3 _test_cli_collage.py`、`QT_QPA_PLATFORM=offscreen python3 _test_collage_tab.py`、`python3 -m py_compile core/collage_processor.py models/collage_model.py ui/collage_tab.py ui/main_window.py` 均通过；UI 测试覆盖 hero 布局按 5 页一组自动生成 3 张拼图
- AI 背景生成预设重设（实拍参考对齐） ✅ `216f3d8` — 验证: 主会话独立目检样图（教室大屏×学生背影×绿幕，竖构图、横幅国旗黑板边、红领巾背影，风格与用户 10 张实拍模板吻合），create-template method=greenscreen 识别成功；组合联动裁剪消灭不契合排列，绿幕开关默认开，offscreen 冒烟通过
- 绿幕 GUI 接入 + 合成清晰度修复 ✅ `f302f69` — 验证: 建模板背景自动放大（屏幕宽 665→1600px，压缩比 2.9:1→1.2:1，Laplacian 39.8→395 约 10 倍），绿边复查 0；GUI「自动识别」绿幕优先，冻结 app 自测（与 GUI 同路由）返回像素级规整矩形；打包 dist/融景.app + DMG 已重建
- 绿幕背景检测路径（生成端消灭识别难题） ✅ `addc4c5` — 验证: 主会话独立复核——Codex 按绿幕 prompt 生成教室图，create-template 走 greenscreen 路径 0.2s 像素级出角点（method 字段可查），合成图边界环带绿色像素 0（外扩 8px 实测值），29 旧模板回归逐位一致。方向：新背景一律绿幕生成，识别从「猜边界」变「找绿色矩形」，永不失手
- VLM 粗框融合识别 + 精修保护 ✅ `8ad8b75` — 验证: 主会话独立复跑基准——精修保护 2 项改善（34.2→26.1px、15.9→7.0px）零回归；VLM 融合使 151038 由 223.6px 降至 112.3px，VLM 不可用时与纯经典逐位一致；全量识别效果预览图（黄=手标/红=自动）已生成供用户目检，20 良好 / 7 仍需手动微调。遗留：近黑关屏笔记本类为各方法共同天花板（P3.B）；模板「5」手标角点顺序错位待用户裁决是否修正
- CLI 无 GUI 建模板能力（create-template） ✅ `f0e5e68` — 验证: 端到端真跑一轮（codex-image-gen 生成教室背景 → create-template 自动识别建模板 → process 合成样例图正确嵌入），预览图角点贴合屏幕；识别失败路径非零退出已验证；测试产物清理后 list-templates 与测试前一致。后续 skill 接入（zhifa-pipeline 前置步骤）未排期，等用户裁决
- 自动角点识别精度重写（多候选+统一打分+角点精修） ✅ `dddd7f1` — 验证: 28 模板基准（用户手标角点为准）识别失败 2→0、≤25px 占比 25%→43%、>80px 跑偏 9→5、无回归项（主会话独立复跑基准逐条核对）；剩余难点近黑关屏笔记本类低对比场景，升级路径见未排期 P3.B
- 打包 app 自动角点识别失效修复（cv2 引导递归） ✅ `8b894c6` — 验证: 冻结 app 内 `RONGJING_DETECT_SELFTEST` 自测返回正确角点坐标（源码/打包结果一致 `[[0,250],[1075,406],[1069,991],[0,1115]]`），无 `detect_error.log`；根因是 `main.py` 的 `sys.path.insert(0,...)` 在冻结环境把 Frameworks 顶到首位遮蔽 cv2 二进制，详见 CLAUDE.md 踩坑 21
- 第二期 P2（拼图+批次差异化+AI背景+背景图持久化） ✅ `e6416a8` — 验证: A/B/C/D/E 全部子阶段（拼图数据模型、差异化引擎、模板背景图持久化、拼图批处理线程、差异化 UI、主窗口集成、AI 背景 API+UI、文档更新、7 项手动端到端测试）均标记「已完成（2026-05-06）」，机械验收标准见原文快照 [详情 →](roadmap/p2-collage-diversify-ai-background-persistence.md)
- 第三期 P3.A（屏幕区域自动检测） ✅ `528066a` — 验证: OpenCV 边缘检测方案落地，AI 生成纯黑屏幕 ≥95% 识别成功、实拍照片 ≥70% 可用，检测失败不影响手动标注 [详情 →](roadmap/p3a-auto-corner-detection.md)
- V3.5（拼图模块四项改进：差异化对比滑动修复+PPT导入优化+设置页备份恢复+自动拆分布局分离） ✅ `a0e9ac6` — 验证: 4 个子项（P3.5-A/B/C/D）均标「已完成（2026-05-07）」，P3.5-D 按方案 B 简化交付 [详情 →](roadmap/v35-collage-four-improvements.md)
- V3.6（拼图模块体验修复 A-K：自动适配算法修正、PPT导入进度、背景色预设、侧边栏对齐、AI背景中国化、拼图前满后补、UI细节、AI异步化、希沃去手写字、禁止年级文字、批量生成修复） ✅ `4b73393`（末项收口；链上另含 `8f4e67e`/`9452e82`/`4cb4a34`/`d221f8c`/`25f7629`/`0e2737a`/`f788076`/`51eb143`/`62c59b1`/`6d73dea`/`86d5b88`） — 验证: 11 个子项全部标「已完成」并各自带 commit hash [详情 →](roadmap/v36-collage-experience-fixes.md)
- V3.7-A（视频导出并行加速，补录归档） ✅ `423ab54` — 验证: 三段流水线（解码/嵌入/编码三线程并行）+ Mac VideoToolbox 硬件编码 + 帧顺序三道防线，修复丢尾帧 bug [详情 →](roadmap/v37a-video-export-parallel.md)
- V3.7-B（图片批量导出并行加速） ✅ `52fa1be` — 验证: 60 张 1920×1080 实测串行 2.57s → 并行 0.71s，加速约 3.6 倍；关闭差异化时并行版与串行版输出逐字节一致；冷眼审查 6/6 通过 + 混合尺寸 8 线程压测 [详情 →](roadmap/v37b-image-batch-parallel.md)
- V3.8-布局（去水印 Tab 三列布局重构） ✅ `1a11c5d`（+审查修复 `d5e7d58`、显示修复 `39c5cef`） — 验证: git 提交链完整（重构→Codex 对抗审查 3 bug 修复→来源选择与强度文字显示修复），用户 2026-07-21 确认已完工关闭 [详情 →](roadmap/v38-dewatermark-three-column-layout.md)
- V3.8（独立「图片去水印」Tab） ✅ `08f9360` — 验证: 三档强度处理符合预期、元数据全空、强档 PSNR 37.7dB，现有 4 个 Tab 零回归；冷眼审查 6/8 通过 + 1 真实 bug（输入输出同路径静默覆盖原图）已修复 + 1 体验问题作为发现项跟进 [详情 →](roadmap/v38-standalone-dewatermark-tab.md)
- V3.9（文档实拍模板：模板分类与合成类型解耦+文档纸张标注融合算法+AI背景入口扩展+批量导出分类筛选） ✅ `0dfd1f9` — 验证: 4 个子项（A/B/C/D）均标「已完成（2026-06-07）」，模板分类/合成类型解耦、纸面融合三预设、AI 背景入口扩展、分类筛选导出全部验收通过 [详情 →](roadmap/v39-document-paper-templates.md)
- 第一期 P1（模板管理、批量导出、CLI 工具、Skill 集成） ✅ `355418b`（commit hash 缺失待补；取自项目「初始提交」附近，原文未记录具体 hash） — 验证: 详见 `FEATURES.md` 一至十二章节 [详情 →](roadmap/p1-basic-features.md)

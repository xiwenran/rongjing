# 打包输出（不上传到 GitHub，太大了）
dist/
build/
*.spec

# Python 缓存
__pycache__/
*.pyc
*.pyo
*.pyd

# macOS 系统文件
.DS_Store

# 本地运维脚本（个人用，不上传）
同步到GitHub.command

# Claude 项目开发笔记（内部用，不上传）
CLAUDE.md

# 自动生成的构建信息（每次 commit 都变，不需要追踪）
_build_info.py

# 编辑器
.vscode/
*.swp
templates/*.json
